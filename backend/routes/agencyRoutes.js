import { Router } from 'express'; import { autocomplete, createAgency, deleteAgency, getAgency, listAgencies, updateAgency } from '../controllers/agencyController.js'; import { adminOnly, optionalAuth, protect } from '../middleware/authMiddleware.js';
const router = Router(); router.get('/', optionalAuth, listAgencies); router.get('/autocomplete', autocomplete); router.get('/:id', optionalAuth, getAgency); router.post('/', protect, adminOnly, createAgency); router.put('/:id', protect, adminOnly, updateAgency); router.delete('/:id', protect, adminOnly, deleteAgency); export default router;
