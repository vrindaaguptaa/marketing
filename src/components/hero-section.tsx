import { ArrowRight, PlayCircle, Search, Sparkles } from 'lucide-react';
import { SERVICES } from '@/lib/constants';
import { useState } from 'react';
interface HeroSectionProps { onSearch: (services: string[], query: string) => void; totalAgencies?: number; }
export function HeroSection({ onSearch, totalAgencies = 0 }: HeroSectionProps) {
  const [selectedServices, setSelectedServices] = useState<string[]>([]); const [searchQuery, setSearchQuery] = useState('');
  const toggle = (service: string) => setSelectedServices((current) => current.includes(service) ? current.filter((item) => item !== service) : [...current, service]);
  const search = () => { onSearch(selectedServices, searchQuery); document.getElementById('agencies-section')?.scrollIntoView({ behavior: 'smooth' }); };
  return <section className="relative isolate overflow-hidden bg-slate-950 px-4 pb-14 pt-16 text-white md:pb-20 md:pt-24"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(37,99,235,.38),transparent_28%),radial-gradient(circle_at_80%_15%,rgba(139,92,246,.30),transparent_25%),linear-gradient(130deg,#020617,#0f172a_55%,#172554)]" /><div className="absolute -left-20 top-20 h-56 w-56 rounded-full border border-white/10 bg-blue-400/10 blur-2xl" /><div className="absolute -right-16 bottom-0 h-72 w-72 rounded-full bg-violet-500/15 blur-3xl" /><div className="relative mx-auto max-w-5xl text-center"><div className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1.5 text-xs font-semibold text-blue-100 backdrop-blur"><Sparkles className="h-3.5" /> Worldwide agency marketplace</div><h1 className="mx-auto mt-6 max-w-4xl text-4xl font-extrabold tracking-tight sm:text-5xl md:text-6xl">Find the Perfect <span className="bg-gradient-to-r from-blue-300 via-cyan-200 to-violet-300 bg-clip-text text-transparent">Marketing Agency</span></h1><p className="mx-auto mt-5 max-w-2xl text-base leading-7 text-slate-300 md:text-lg">Compare verified agencies, AI rankings, reviews and services in one place.</p><div className="mx-auto mt-8 max-w-4xl rounded-2xl border border-white/15 bg-white/10 p-3 shadow-2xl backdrop-blur-xl"><div className="flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-4 top-3.5 h-5 text-slate-400" /><input value={searchQuery} onChange={(event) => setSearchQuery(event.target.value)} onKeyDown={(event) => event.key === 'Enter' && search()} placeholder="Search agencies, services, cities or countries" className="w-full rounded-xl border border-white/10 bg-white px-11 py-3 text-slate-900 outline-none ring-primary transition placeholder:text-slate-400 focus:ring-2" /></div><button onClick={search} className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-6 py-3 font-semibold transition hover:bg-blue-500 hover:shadow-lg">Explore Agencies <ArrowRight className="h-4" /></button></div><div className="mt-3 flex flex-wrap items-center justify-center gap-2">{SERVICES.slice(0, 6).map((service) => <button key={service} onClick={() => toggle(service)} className={`rounded-full px-3 py-1.5 text-xs font-semibold transition ${selectedServices.includes(service) ? 'bg-white text-primary' : 'bg-white/10 text-slate-200 hover:bg-white/20'}`}>{service}</button>)}</div></div><div className="mt-6 flex flex-wrap justify-center gap-3"><a href="#agencies-section" className="inline-flex items-center gap-2 rounded-xl bg-white px-5 py-3 text-sm font-semibold text-slate-950 transition hover:bg-blue-50">Explore Agencies <ArrowRight className="h-4" /></a><a href="#marketplace-insights" className="inline-flex items-center gap-2 rounded-xl border border-white/20 bg-white/5 px-5 py-3 text-sm font-semibold text-white transition hover:bg-white/10"><PlayCircle className="h-4" /> How It Works</a></div><p className="mt-7 text-sm text-slate-400"><span className="font-bold text-white">{totalAgencies.toLocaleString()}+</span> verified agencies across a global network</p></div></section>;
}
